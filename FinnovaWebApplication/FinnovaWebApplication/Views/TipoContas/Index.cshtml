@model IEnumerable<FinnovaWebApplication.Models.TipoConta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DescricaoTipoConta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DescricaoTipoConta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoConta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoConta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoConta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
